<% layout('layouts/boilerPlate') %>

<article class="contact active" data-page="update-skills">
    <header>
        <h2 class="h2 article-title">Update Skills</h2>
    </header>

    <% if (action === 'add') { %>
    <section class="contact-form">
        <h3 class="h3 form-title">Add New Skill</h3>
        <form action="/updating/skill/add" method="POST" class="form">
            <div class="input-wrapper">
                <input type="text" name="name" class="form-input" placeholder="Skill Name (e.g., Web Development)" required>
                <input type="text" name="tools" class="form-input" placeholder="Tools (comma-separated)" required>
            </div>
            <button type="submit" class="form-btn" style="margin-top: 1rem;">
                <ion-icon name="add-outline"></ion-icon>
                <span>Add Skill</span>
            </button>
        </form>
    </section>
    <% } else if (action === 'edit') { %>
    <section class="contact-form">
        <h3 class="h3 form-title">Edit Skill</h3>
        <form action="/updating/skill/edit/<%= data._id %>?_method=PUT" method="POST" class="form">
            <div class="input-wrapper">
                <input type="text" name="name" class="form-input" value="<%= data.name %>" required>
                <input type="text" name="tools" class="form-input" value="<%= (data.tools || []).join(', ') %>" required>
            </div>
            <button type="submit" class="form-btn" style="margin-top: 1rem;">
                <ion-icon name="checkmark-outline"></ion-icon>
                <span>Update Skill</span>
            </button>
        </form>
    </section>
    <% } else if (action === 'delete') { %>
    <section class="service">
        <h3 class="h3 service-title">Delete Skill</h3>
        <% if (data && data.length > 0) { %>
        <ul class="service-list">
            <% data.forEach(item => { %>
            <li class="service-item">
                <div class="service-content-box">
                    <h4 class="h4 service-item-title"><%= item.name %></h4>
                    <p class="service-item-text"><%= item.tools.join(', ') %></p>
                    <div style="display: flex; gap: 1rem; margin-top: 1rem;">
                        <a href="/updating/skill/edit/<%= item._id %>" class="form-btn" style="flex: 1; text-align: center;">
                            <ion-icon name="create-outline"></ion-icon>
                            <span>Edit</span>
                        </a>
                        <form action="/updating/skill/delete/<%= item._id %>?_method=DELETE" method="POST" style="flex: 1;">
                            <button type="submit" class="form-btn" style="width: 100%;">
                                <ion-icon name="trash-outline"></ion-icon>
                                <span>Delete</span>
                            </button>
                        </form>
                    </div>
                </div>
            </li>
            <% }); %>
        </ul>
        <% } else { %>
        <p>No items found.</p>
        <% } %>
    </section>
    <% } %>
</article>
